
---

# 📕 README.IR.md (فارسی)


# PD-3x-ui-Cleaner

ابزار حرفه‌ای **مدیریت و پاکسازی لاگ‌ها** مخصوص سرورهای دارای **3x-ui / Xray**  

این اسکریپت برای جلوگیری از پر شدن بی‌صدای دیسک توسط لاگ‌ها (خصوصاً `journald`) طراحی شده است.

---

## ✨ امکانات

- 🧹 پاکسازی امن لاگ‌های 3x-ui، Xray و سیستم
- 📊 نمایش میزان مصرف دیسک قبل و بعد از پاکسازی
- 🧪 حالت پیش‌نمایش (Dry-run) بدون حذف واقعی
- 📦 نمایش مسیرهای پرمصرف دیسک
- ⏲ اجرای خودکار با **systemd timer** (بدون cron)
- 🔁 تعیین فاصله اجرا (روزانه یا هر N روز)
- 🧾 کنترل و محدودسازی journald (rotate + vacuum)
- 🧠 نمایش وضعیت کامل سیستم و تایمر
- ❌ حذف کامل اسکریپت با یک دستور

---

## 🚀 نصب

روی سرور خود دستور زیر را اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/Mehdi682007/PD-3x-ui-Cleaner/main/install.sh)
```

این دستور:

اسکریپت را دانلود می‌کند

اجرایی می‌کند

و آن را اجرا می‌کند

🖥 استفاده

منوی زیر نمایش داده می‌شود:
1) Install / Update
2) Clean logs now
3) Dry-run preview
4) Show status
5) Top disk consumers
6) Set auto-run (systemd timer)
7) Remove script
8) Exit



⏲ اجرای خودکار (systemd timer)

فقط از systemd timer استفاده می‌شود

cron به‌طور کامل حذف شده

اجرای روزانه یا هر چند روز یکبار

بررسی وضعیت:


systemctl list-timers | grep 3xui


❓ چرا لاگ‌ها کامل صفر نمی‌شوند؟

در لینوکس، journald همیشه چند فایل فعال نگه می‌دارد.
این اسکریپت:

لاگ‌ها را rotate می‌کند

حجم را محدود می‌کند

از رشد بی‌رویه جلوگیری می‌کند

این رفتار کاملاً استاندارد و صحیح است.

❌ حذف اسکریپت

از داخل منو گزینه:


Remove script

را انتخاب کنید.

این کار:

تایمر systemd را غیرفعال می‌کند

فایل‌های سرویس را حذف می‌کند

خود اسکریپت را پاک می‌کند

بدون آسیب به لاگ‌ها یا سیستم.

✅ مناسب برای

سرورهای 3x-ui

VPSهای همیشه روشن

سرورهای کم‌حجم

محیط‌های عملیاتی واقعی

📄 لایسنس

MIT License

تهیه و نگهداری: Mehdi

محیط اسکریپت :



<img width="2343" height="1354" alt="image" src="https://github.com/user-attachments/assets/89b334de-4e79-469e-aa5a-432bc11e61ec" />





